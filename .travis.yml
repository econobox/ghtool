jobs:
  include:
    - stage: danger
      language: ruby
      rvm: 2.5.1
      install: bundle install
      script: bundle exec danger
    - stage: test
      language: rust
      rust: stable
      cache: cargo
    - language: rust
      rust: beta
    - language: rust
      rust: nightly
  allow_failures:
    - rust: nightly
  fast_finish: true

stages:
  - danger
  - test

notifications:
  email: false
